---
name: Setup ImageMagick
description: Download the latest ImageMagick binary and add it to the PATH
author: Mario Finelli
inputs:
  cache:
    description: Cache the magick binary (ubuntu only)
    required: false
    default: true
  install-libfuse2:
    description: Ensure libfuse2 is installed (Ubuntu only)
    required: false
    default: true
runs:
  using: node24
  main: dist/index.js
branding:
  icon: image
  color: purple
